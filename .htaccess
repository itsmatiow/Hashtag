RewriteEngine On

# Redirect www to non-www
RewriteCond %{HTTP_HOST} ^www\.hashtagteam\.ir$ [NC]
RewriteRule ^(.*)$ https://hashtagteam.ir/$1 [L,R=301]

RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

RewriteRule ^ index.html [L]
